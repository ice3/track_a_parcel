============
Installation
============

At the command line::

    $ easy_install track_a_parcel

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv track_a_parcel
    $ pip install track_a_parcel